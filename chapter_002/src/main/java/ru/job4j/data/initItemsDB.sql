CREATE TABLE IF NOT EXISTS item (id INTEGER PRIMARY KEY, name VARCHAR(30) NOT NULL, description VARCHAR(100), createDate VARCHAR(20) NOT NULL, type VARCHAR(20) NOT NULL, FOREIGN KEY (type) REFERENCES type(name));