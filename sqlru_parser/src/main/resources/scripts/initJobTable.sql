CREATE TABLE IF NOT EXISTS job_offer (id INTEGER PRIMARY KEY AUTOINCREMENT, author TEXT NOT NULL, title TEXT NOT NULL, content TEXT NOT NULL UNIQUE, post_timedate TEXT NOT NULL, url TEXT NOT NULL, isLast INTEGER DEFAULT 0);